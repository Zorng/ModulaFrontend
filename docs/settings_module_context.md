# (Archived) Settings module context (legacy)

This legacy document moved to `docs/_archive/legacy_module_notes/settings_module_context.md`.

Canonical spec: `docs/modSpec/branch_module.md` and related module specs (if/when split).


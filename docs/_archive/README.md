# `docs/_archive/`

This folder contains **deprecated or legacy** product docs that are kept for history.

Rules:
- Prefer linking to canonical specs (`docs/modSpec/`, `docs/apiContracts/`) from archived docs.
- If an archived doc is still referenced by Jira/refactor plans, keep a small stub at the old path pointing here.


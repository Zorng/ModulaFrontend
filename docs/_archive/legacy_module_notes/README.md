# Legacy Module Notes (Archived)

These documents were written before `docs/modSpec/` and `docs/apiContracts/` became the canonical sources.

They are kept for history and context only.

Preferred canonical sources:
- Module specs: `docs/modSpec/`
- API contracts: `docs/apiContracts/`


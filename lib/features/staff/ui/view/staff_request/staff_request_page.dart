import 'package:flutter/material.dart';

class StaffRequestPage extends StatelessWidget {
  const StaffRequestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('No request data yet')));
  }
}

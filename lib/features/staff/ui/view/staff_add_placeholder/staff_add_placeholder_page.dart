import 'package:flutter/material.dart';

class StaffAddPlaceholderPage extends StatelessWidget {
  const StaffAddPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Coming soon'), centerTitle: false),
      body: const Center(child: Text('Coming soon')),
    );
  }
}
